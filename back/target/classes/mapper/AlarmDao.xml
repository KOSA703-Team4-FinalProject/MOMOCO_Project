<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.dao.AlarmDao">

	<!-- 알람 리스트 불러오기 public List<Alarm> getAlarmList(Alarm alarm) -->
	<select id="getAlarmList" parameterType="kr.or.vo.Alarm" resultType="kr.or.vo.Alarm" >
		select m.u_idx, nickname, profilephoto, a_idx, idx, content, check_alarm, url
		from member m right join (select a_idx, idx, content, check_alarm, url, u_idx
	    		                    from alarm where u_idx = 8349037) a
		on m.u_idx = a.u_idx
		order by a_idx
	</select>

</mapper>